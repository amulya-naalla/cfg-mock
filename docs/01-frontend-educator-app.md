# Frontend: Educator App

Owner: A

## Scope

- App shell, routing, and navigation (`frontend/src/main.jsx`, `App.jsx`)
- Student list and detail views
- New assessment form
- Shared API client (`frontend/src/api/client.js`)

## Notes

- `App.jsx` is shared by everyone — keep edits there small and announce them in chat.
- Set `VITE_API_BASE_URL` in `frontend/.env` to match B's current backend URL (e.g. an ngrok tunnel).
